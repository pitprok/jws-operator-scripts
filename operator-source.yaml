apiVersion: operators.coreos.com/v1
kind: OperatorSource
metadata:
  name: web-servers-operators
  namespace: openshift-marketplace
spec:
  type: appregistry
  endpoint: https://quay.io/cnr
  registryNamespace: web-servers
  displayName: "JWS Operator"
  publisher: "web-servers"